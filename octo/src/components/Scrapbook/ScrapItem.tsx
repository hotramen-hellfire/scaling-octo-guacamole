import React from 'react';
import { scrap } from '../../../interfaces';

type ScrapItemProps = {
    scrap: scrap
};

const ScrapItem: React.FC<ScrapItemProps> = ({ scrap }) => {

    return <div>Have a good coding</div>
}
export default ScrapItem;